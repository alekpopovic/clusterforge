{{ .Header }}

module "rke2" {
  source = "{{ .ModulesPath }}/orchestrators/kubernetes/rke2{{ .ModulesRef }}"

  cluster_name = var.name
  environment  = var.environment
  server_count = var.server_count
}
