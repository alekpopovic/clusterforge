# Production Policy Pack

Advisory and blocking intent:

- remote backend required
- version pinning required
- provider constraints required
- module sources should use tags, not `main`
- public ingress requires explicit approval

Some checks are currently documented guidance until OPA/Checkov rules are
added.
