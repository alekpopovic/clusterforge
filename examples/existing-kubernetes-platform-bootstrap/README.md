# existing-kubernetes-platform-bootstrap

Shows a conservative platform bootstrap for an existing Kubernetes cluster.

```bash
terraform init
terraform plan
```

Review cluster permissions before applying.
