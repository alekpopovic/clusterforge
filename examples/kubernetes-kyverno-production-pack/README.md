# Kyverno production policy pack

This example enables the extended policies in `Audit` mode. Set `enforce=true`
only after reviewing policy reports, documenting exceptions, and testing affected
workloads. Pin a chart version compatible with the cluster.
