# Existing Kubernetes Integration Target

Run with:

```bash
KUBECONFIG=/path/to/kubeconfig CLUSTERFORGE_RUN_INTEGRATION_TESTS=true scripts/integration-test.sh existing-kubernetes
```

Use only disposable clusters or namespaces.
