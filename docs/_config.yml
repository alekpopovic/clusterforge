title: ClusterForge
description: Readable Terraform/OpenTofu platform engineering for Kubernetes, ECS, Nomad, and Docker.
baseurl: "/clusterforge"
url: "https://alekpopovic.github.io"
repository: "alekpopovic/clusterforge"
release: "v0.4.0"
logo: "/assets/clusterforge-logo.svg"

markdown: kramdown
highlighter: rouge

defaults:
  - scope:
      path: ""
    values:
      layout: default

exclude:
  - README.md
